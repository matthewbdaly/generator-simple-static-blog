language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-simple-static-blog' ]; then cd .. && eval "mv $currentfolder generator-simple-static-blog" && cd generator-simple-static-blog; fi

